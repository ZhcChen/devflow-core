# 测试报告

## 命名规范

```
TEST-{id}.md
```

- `id` 为 6 位自增数字，例如 `000001`

## 模板

- [templates/test.md](../templates/test.md)

## 用例ID规则

```
TC-{id}-{seq}
```

- `id` 为 6 位任务编号，例如 `000001`
- `seq` 为 3 位序号，例如 `001`
